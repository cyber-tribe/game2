# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Codeが従うべき方針を記載する。

## Pull Requestの粒度

- **意味のある変更ごとに1つのPRを作成すること。** 複数の独立した変更
  （例：技術選定、機能実装、リファクタリング、ドキュメント更新）を
  1つのPRにまとめない。
- 1つのPRは、それ単体でレビュー・差し戻し・マージ可能な最小のまとまりに保つこと。
- 関連はしているが独立して評価できる変更（例：新機能の実装と、それとは
  無関係な既存バグの修正）は分割する。
- 逆に、1つの変更を意味のない単位に細切れにする必要はない
  （例：型定義の追加とそれを使う実装を別PRにする、など）。
