# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Codeが従うべき方針を記載する。

## Pull Requestの粒度

- **意味のある変更ごとに1つのPRを作成すること。** 複数の独立した変更
  （例：技術選定、機能実装、リファクタリング、ドキュメント更新）を
  1つのPRにまとめない。
- 1つのPRは、それ単体でレビュー・差し戻し・マージ可能な最小のまとまりに保つこと。
- 関連はしているが独立して評価できる変更（例：新機能の実装と、それとは
  無関係な既存バグの修正）は分割する。
- 逆に、1つの変更を意味のない単位に細切れにする必要はない
  （例：型定義の追加とそれを使う実装を別PRにする、など）。

## 実装記録の置き場所

- 機能実装や不具合対応の経緯・設計判断・検証方法は、`plan/`直下に
  そのタスク専用の独立したMarkdownファイル（`NNNN-短い英語スラッグ.md`）
  として記録すること。1つの共有ファイルへの追記は行わない
  （同じ挿入位置を複数PRが奪い合いマージコンフリクトが頻発するため）。
- 対応が完了しPRがマージされたら、そのファイルを`plan/archived/`へ移動する。
- 詳しい運用ルールは`plan/README.md`を参照。
- 技術選定やプロジェクト構成など、頻繁には変わらない参照用の情報は
  `docs/architecture.md`に置く。
